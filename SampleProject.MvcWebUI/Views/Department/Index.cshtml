@model List<SampleProject.MvcWebUI.Models.DepartmentViewModel>
@{
    ViewBag.Title = "Index";
}

<div class="container">
    <div class="row">
        <table class="table table-bordered table-responsive table-hover">
            <thead>
                <tr>
                    <th>Department Name</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var departmentList in Model)
                {
                    <tr>
                        <td>@departmentList.DepartmentName</td>
                    </tr>
                }
            </tbody>
        </table>
        <div>
            @Html.ActionLink("Back to List", "Index", "Home", null, new { @class = "btn btn-warning" })
        </div>
    </div>
</div>





